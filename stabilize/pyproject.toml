[project]
name = "stabilize"
version = "0.1.0"
description = "Moon video tracking and stabilization pipeline"
requires-python = ">=3.12"
dependencies = [
    "opencv-python",
    "numpy",
    "scipy",
    "tqdm",
]
